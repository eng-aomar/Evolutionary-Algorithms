'Implementation of Algorithm number 31
'Propotionate Method
Public Class StochasticUniversalSampling : Implements ISelection(Of BitArray)

    Public Function DoSelection(population As Population) As BitArray Implements ISelection(Of BitArray).DoSelection
        Throw New NotImplementedException()
    End Function
End Class
